version: '3'
services:
 wordki:
  container_name: wordki
  image: wordki
  build:
   context: ../../
   dockerfile: ./src/Wordki.Api/Dockerfile.Production
  ports:
   - "5000:5000"
  env_file:
   - secrets.Production.env
  network_mode: "host"
  
