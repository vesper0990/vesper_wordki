version: '3'
services:
 mysql:
  container_name: wordki_tests
  image: mysql/mysql-server:5.7
  restart: always
  ports:
   - "3306:3306"
  environment:
   MYSQL_RANDOM_ROOT_PASSWORD: asdf
  env_file:
   - secrets.env
