version: '3'
services:
  wordki-client.ci.test:
    container_name: wordki-client.ci.release
    image: wordki-client/ci/release
    build:
      context: ../../
      dockerfile: ./Dockerfile.production
      target: release
