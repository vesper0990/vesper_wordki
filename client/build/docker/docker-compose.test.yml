version: '3'
services:
  wordki-client.ci.test:
    container_name: wordki-client.ci.test
    image: wordki-client/ci/build
    build:
      context: ../../
      dockerfile: ./Dockerfile.production
      target: build
    command: sh -c "npm run test-headless"
    environment:
      - TEST_RESULTS_DIR=/test-artifacts
    volumes:
      - $COMMON_TESTRESULTDIRECTORY:/test-artifacts
