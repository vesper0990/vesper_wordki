export class Group {
    constructor(public id: number,
        public name: string,
        public language1: number,
        public language2: number,
        public wordsCount: number, ) {
    }
}
