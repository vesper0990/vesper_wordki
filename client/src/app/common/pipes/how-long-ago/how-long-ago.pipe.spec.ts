import { HowLongAgoPipe } from './how-long-ago.pipe';

describe('LastLessonPipe', () => {
  it('create an instance', () => {
    const pipe = new HowLongAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
